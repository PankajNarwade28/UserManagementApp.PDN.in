<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>edit page</title>
    <style>
      body {
        display: flex;
        justify-content: center;
        align-items: center;
        flex-direction: column;
        font: 300;
        font-family: "Gill Sans", "Gill Sans MT", Calibri, "Trebuchet MS",
          sans-serif;
      }
      span {
        color: orange;
      }
      textarea {
        color: rgb(244, 20, 20);
        font-size: 1.5rem;
      }
      h1 {
        color: orange;
        text-align: center;
      }
      form {
        margin-top: 20px;
        border: 2px solid rgb(129, 117, 117);
        display: flex;
        padding: 30px;
        height: 500px;
        width: 500px;
        flex-direction: column;
        justify-content: center;
        align-items: center;
      }
      input {
        width: 90%;
        height: 30px;
        margin: 20px;
        color: red;
      }
      input:hover,
      input:active,
      textarea:hover,
      textarea:active {
        box-shadow: 8px 8px 8px whitesmoke;
      }
      input,
      .btn {
        width: 80%;
        height: 30px;
        padding: 12px;
        margin: 25px;
        color: rgb(3, 2, 2);
      }
      .btn {
        border: 1px solid rgb(15, 15, 15);
        text-align: center;
        padding: 5px;
        background-color: #fff;
        margin-top: 2px;
        margin-bottom: 2px;
        height: 50px;
        width: 85%;
      }
      .btn:hover {
        cursor: pointer;
        color: rgb(8, 20, 7);
        background-color: rgb(124, 255, 63);
      }
    </style>
  </head>
  <body>
    <form method="post" action="/user/<%= user.id %>?_method=PATCH" name="">
      <h1>Editing the content of <br /><span><%= user.email %></span></h1>
      <hr />
      <textarea name="username" id="" cols="30" rows="10">
        <%= user.username %>
        </textarea
      ><br />
      <input
        type="password"
        name="password"
        id=""
        placeholder=" Enter password"
      />
      <button class="btn">Submit</button>
    </form>
  </body>
</html>
